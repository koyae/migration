mousetrack on
vbell off
defscrollback 10000
term xterm-256color

# ctrlC creates a new task and immediately offers to rename it, clearing the
# preset name automatically:
bind ^c eval screen "title ''" title
bind "" copy

layout new
screen
split -v
focus
screen
