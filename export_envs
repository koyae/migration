#!/bin/bash
# ~/export_envs

#
# Export cross-system and local machine settings
#
export HISTIGNORE="ls" # don't store plain ls-commands in bash history
export HISTFILESIZE=100000
export LS_COLORS=$LS_COLORS:'di=0;34:ln=0;36:ex=0;32:' 
#^ blue directories, cyan symlinks, green executables 
# http://linux-sxs.org/housekeeping/lscolors.html
# http://askubuntu.com/a/466203/432173 
  
export -f up
export -f sis
export -f cdl
export -f lcd
export -f unlock
export -f grepr
